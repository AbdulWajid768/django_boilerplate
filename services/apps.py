"""Services app config."""
from django.apps import AppConfig


class ServicesConfig(AppConfig):
    """Default config for the services app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'services'
